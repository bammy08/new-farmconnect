/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io', 'images.unsplash.com'], // Add the domain of the external image host
  },
};

export default nextConfig;
